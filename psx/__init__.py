"""The PlayStation itself, with nothing of Tomba! 2 in it.

A MIPS interpreter and the GPU/VRAM model the game's own code is run
against (see game/actor_sim.py), plus the VRAM viewer built on top.
"""
