"""What is true of Tomba! 2 in particular, rather than of any PSX game.

Where the placements are, which model a handler draws, what each file
on the disc is called, how the areas differ between the eleven builds -
and actor_sim, which answers those by running the game's own code on
the interpreter in psx/.
"""
